# Python 依赖
# 安装命令：pip3 install -r requirements.txt

# ROS2 相关依赖通常通过 apt 安装，不在 pip 中
# sudo apt install ros-${ROS_DISTRO}-cv-bridge ros-${ROS_DISTRO}-vision-msgs

# 相机标定和离线测试
opencv-python>=4.8.0
numpy>=1.24.0
PyYAML>=6.0

# 可选：ONNX 运行时（用于 PC 端测试）
# onnxruntime>=1.15.0
